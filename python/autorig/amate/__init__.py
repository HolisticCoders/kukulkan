"""Amate serves as Kukulkan nodal interface for rigs representation.

ENV:

    This package makes use of the AMATE_DB environment variable
    to find the user database.
    If this variable is not specified, then the database used
    will be the built-in one.
    No update to the Amate data is allowed if no user database
    is specified.
"""
